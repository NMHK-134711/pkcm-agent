# 인수인계 — 2026-09-06, 개인PC 작성

랩실PC 앞으로. 이 문서는 **오늘 무엇이 바뀌었고 다음에 무엇을 돌리는가**만 적는다.
이전 판(09-03, 랩실 작성)은 커리큘럼 게이트 기록이었는데, 그 숫자는 아래 §1
때문에 전부 다른 엔진 위의 것이다. 필요하면 git 이력에 남아 있다.

## 0. 부탁 — 지금 돌려주셨으면 하는 것

```
python scripts/tournament.py --parties data/champions/parties_field.json \
    --repeats 2 --search-iterations 800 --preview-iterations 3200 \
    --workers 10 --out runs/field_rr_800.json
```

253파티 전체 라운드로빈. 31,878 대진 × 4게임 = 127,512판, 10코어로 **약 40시간**
(개인PC 20코어 실측 106게임/분을 절반으로 환산). 화요일 아침까지 20시간 여유.

- **끊겨도 된다.** 대진마다 `runs/field_rr_800.jsonl`에 한 줄씩 쌓이고, 같은 명령을
  다시 치면 이미 둔 것을 건너뛴다. 씨드가 대진에서 나오므로 나눠 돌린 조각이 한
  측정으로 붙는다.
- 30초마다 남은 시간을 시간 단위로 찍는다.
- 필요한 파일은 `data/champions/parties_field.json` 하나뿐이고 커밋에 있다.
  `data/raw/` 캐시(HTML 442MB)는 `.gitignore`라 안 당겨진다. 다시 받을 필요 없다.

**설정 근거.** 800시뮬은 이전 라운드로빈들과 같은 예산이라 비교가 된다. 프리뷰
3200은 §2의 +2.3%p짜리로, "실제로 배포할 에이전트"로 재기 위한 것이다. 쌍당
4게임은 2게임보다 꼬리 편향이 절반이라서인데, §4를 함께 읽을 것.

### 0b. 두 기계가 번갈아 돈다 — 랩실이 추가 (09-06 16:20)

**랩실 실측이 문서 추정의 2.5배다.** 21게임/분, 남은 시간 101시간. 개인PC 20코어
106게임/분을 코어 수로만 반절한 40시간 추정에는 코어 자체의 속도 차가 빠져 있었다
(09-02 실측: 자가대전 기준 개인PC가 3.8배 빠름). 랩실 단독으로는 목요일 저녁이다.

그래서 hk의 결정: **낮에는 랩실, 밤에는 개인PC**로 번갈아 돌리고 GitHub로 주고받는다.
밤 10시간씩 두 번이면 화요일 아침에 끝난다.

이어받기가 기계를 건너도 성립하는 근거는 `play_fixture`의 `seed = config.seed + repeat`
— 시드가 스케줄 위치도 기계도 아닌 **대진에서** 나오므로, 어느 쪽이 둔 대진이든 같은
게임이고 두 조각은 한 측정이다. 재개는 `(a, b, repeat)` 딕셔너리라 중복 줄도 무해하다.

**절차 — 돌리기 전에 반드시 pull.**

```bash
git pull                                   # 상대가 둔 것을 먼저 받는다
python scripts/tournament.py --parties data/champions/parties_field.json \
    --repeats 2 --search-iterations 800 --preview-iterations 3200 \
    --workers <물리 코어> --out runs/field_rr_800.json   # 랩실은 뒤에 --reverse
# 넘길 때
git add runs/field_rr_800.jsonl && git commit -m "field rr: <n> fixtures" && git push
```

- `runs/field_rr_*.jsonl`을 `.gitignore` 예외로 넣었다. 다 차면 5MB쯤.
- `--parties`·`--repeats`·`--search-iterations`·`--preview-iterations`는 **양쪽이 같아야**
  한다. `--workers`만 기계에 맞춘다.
- **양 끝에서 동시에 돌린다.** 랩실은 `--reverse`를 붙여 마지막 대진부터 거꾸로
  올라오고, 개인PC는 그대로 앞에서 내려간다. 둘이 중간에서 만나면 끝이다.
  개인PC 실측 40대진/분, 랩실 10대진/분이라 만나는 지점은 전체의 80% 근처다.
  겹치는 구간은 재개 딕셔너리가 `(a, b, repeat)`로 걸러내므로 무해하고, 한쪽이
  pull하는 순간 사라진다. **`--reverse` 없이 둘이 같이 돌리면 처음부터 통째로
  중복 계산한다.**
- 완주하면 `runs/field_rr_800.json`(순위·전체 fixture)이 나온다. 그 전에 중단한
  부분표본은 편향돼 있다 — 대진이 파티 0부터 순서대로 진행되므로 앞 파티만 채워진다.
  중간 결과를 읽어야 할 일이 생기면 시드 고정 셔플로 바꾸면 되고, 이미 둔 대진은
  그대로 재사용된다.

## 1. 엔진에서 다섯 가지가 틀려 있었다 — 전부 "실제보다 강한 쪽"

**오늘 이전의 모든 승률 측정은 다른 엔진 위의 숫자다.** hk의 기존 원칙(구엔진은
무의미)이 그대로 걸린다.

| 무엇 | 어떻게 틀렸나 | 커밋 |
|---|---|---|
| 기습 | 실패 조건이 아예 없어 항상 성공. 위력 70 +1이 공짜였다 | `7a9218f` |
| 속임수·선제일격·어퍼핸드·잠꼬대·비장의무기 | 조건이 쇼다운 핸들러 코드에만 있어 `move_support`가 "완전 지원"으로 보고. 매 턴 사용 가능했다 | `adfb672` |
| 브래킷 안 순서 | 선제의발톱·퀵드로가 우선도 자체에 +1을 더해 몸통박치기가 아쿠아제트를 이겼다. 끈기는 "아무것도 안 하는 특성" 목록에 있었다 | `adfb672` |
| 회피율 조항 | `mods/champions/rulesets.ts`의 세 조항 전부 게임에 없다. 회피만 마지막까지 남아 있었다 | `1b7099f` |
| 확정 명중 | 노가드·락온이 "확정"을 명중률 100%로 표현해 회피 단계가 그 위에 곱해졌다. 작아진 상대에게 위력 2배인 기술들은 확정 명중이 없었다 | `e679526` |

마지막 둘은 이어져 있다. 회피 조항이 걸려 있는 동안 **어떤 팀도 회피 기술을 못
들었으므로 그 아래 코드가 한 번도 안 돌았다.** 조항을 풀자마자 드러났다.

기습은 파티 39의 대도각참·입치트가 둘 다 들고 있다. 그 파티 위의 지금까지 모든
숫자가 그만큼 부풀어 있었다.

**교훈은 출처에 대한 것이다.** 세 조항 전부 쇼다운 mod에서 왔고 셋 다 틀렸다.
쇼다운은 메커닉 참조지 규칙 출처가 아니다 —
`test_sleep_and_ohko_moves_are_allowed_on_a_team`에 적어뒀다. 지금도 mod를
참조하는 다섯 군데는 전부 메커닉이다(PP 상한 20, pp×8/5, 메가 폼 영구 지속,
다단히트 루프, 상태이상 이식). 규칙으로 신뢰하던 자리는 이제 없다.

## 2. 프리뷰 픽에 예산을 주면 +2.3%p

4000게임씩 네 번, 프리뷰 노드 말고는 전부 동일.

```
절단 120 대 24    45.0% [43.5, 46.6]   분리됨 (나쁜 쪽)
절단  48 대 24    48.0% [46.5, 49.6]   분리됨 (나쁜 쪽)
절단  12 대 24    50.4% [48.8, 51.9]   분리 안 됨
예산  ×4 대 1×    52.3% [50.8, 53.9]   분리됨
```

후보를 넓히는 건 단조롭게 손해다. 3200시뮬에서도 그대로라 예산 부족이 아니다.
`max_branching`은 평평한 구간에 있어 더 만질 게 없다. 대신 남긴 24개를 오래
생각하게 하면 이긴다.

`SearchConfig.preview_iterations`. **기본값은 `None`이라 기존 측정은 안 바뀐다.**
`coach.py`만 기본으로 4배를 쓰고, `tournament.py`는 플래그로 받되 기본은 꺼져 있다.

앞선 감사도 같이: 챔피언스의 0 아닌 우선도 기술 35개를 전부 턴 순서 키와 대조해
불일치 0건. 기술 우선도 자체는 처음부터 정상이었다.

## 3. 필드가 46 → 253파티

`data/champions/parties_field.json`. 출처(hand/pokesol)·순위·레이팅·원문 링크가
파티마다 붙어 있다.

```
사다리 슬롯 커버리지  82.3% → 98.5%
종족                  54 → 112종
```

pokedb 구축기사 색인 666개를 만들고, 그중 pokesol 241개에서 **208파티를 이미지
없이** 읽었다. 기사 본문의 `data-type="pokemon-card"` 속성에 종족·특성·도구·성격·
기술 4개·배분이 전부 id로 있다. 다만 pokesol은 종족·도구·기술 번호를 pokedb나
우리와 다르게 매기는 곳이 있어 학습셋 제약과 색인 대조로 실증으로 풀었다
(`data/champions/pokesol_{species,items,moves}.json`).

손파티 46개 중 하나(pokedb 05)가 pokesol 랭크 5 기사와 여섯 마리 전부 완전히
같아 하나 뺐다. 기술 순서만 다른 중복은 없었다.

읽지 못한 기사 33개는 `runs/held_articles.md`에 링크·순위·이미 읽어낸 것과 함께
있다. 25개는 글쓴이가 카드 기능을 안 써서 이미지가 필요하다.

## 4. 결과를 읽을 때 — 평균이 아니라 저점

**필드는 순환한다.** 43>14>42>43, 각 변이 통계적으로 분리됨
(`runs/tournament_contenders.json`, 쌍당 80게임). 4항 순환도 있다.
hk의 해석: 순환은 엔진이 상성 구조를 구분할 만큼 정확해졌다는 신호다. 깔끔하게
줄 세워졌으면 그게 걱정할 일이었다. **잡음으로 평균 내지 말 것.**

순환하는 판에서 평균 승률은 "어떤 상대가 필드에 있었나"를 주로 보고한다. hk의
목표가 "웬만한 파티에 대응방법이 있는, 저점이 높은 파티"라 **최악 1/4 대진의
평균(CVaR)** 으로 다시 계산한다. `pkcm.train.party_floor.summarise`가 그 JSON을
그대로 읽는다. `tournament.py`의 화면 출력은 여전히 평균 순이지만, 결과 JSON에
모든 대진이 들어가므로 나중에 뽑으면 된다.

**CVaR은 표본이 적으면 아래로 편향된다.** 꼬리를 고르는 게임과 채점하는 게임이
같아서 그렇다(순서통계량의 승자의 저주). 253파티는 꼬리가 63마리라 쌍당 4게임에도
편향이 꽤 남는다. 그러니 **절대값이 아니라 순위로 읽고**, 상위 몇 개는 개인PC에서
3200시뮬로 확인한다. `tests/test_party_floor.py`에 이 성질을 테스트로 박아뒀다.

## 5. 닫힌 것 — 다시 재지 말 것

- **프리뷰 절단 폭**: 24가 평평한 구간. 넓히면 손해, 좁혀도 변화 없음. §2
- **기술 우선도**: 35개 전부 정상. 틀린 건 브래킷 안 순서였고 고쳐졌다
- **pokesol 기술 번호표**: 267개 중 259개가 우리와 일치. 어긋난 6개는 hk가 기사를
  읽어 확정했다(`data/champions/pokesol_moves.json`). 오프셋이 아니라 개별 차이다
- **개인PC 워커 크래시**: 코드 아님. 기계 문제로 종결(09-03)

## 6. 열려 있는 것

- **회피율 해금의 영향.** 작아지기·그림자분신이 이제 합법이고 변이 연산자가 뽑을 수
  있다. 회피 파티가 실제로 얼마나 강한지 안 재봤다
- **학습셋 대조.** `overqwil`의 작아지기가 조항에 가려져 있었듯 어긋난 곳이 더 있을
  수 있다. hk가 포켓몬 위키의 챔피언스 기술 표로 검증 가능하다고 알려줬다
- **파티 최적화기(B).** 변이 연산자(`party_mutate`)와 벤치마크 연산자
  (`party_benchmark`)와 저점 평가기(`party_floor`)까지 있고, 탐색 루프만 없다.
  §0의 결과가 그 출발점이자 비교 기준이 된다
- **note·하테나 기사 355개.** 이미지 수집을 시도했다가 썸네일만 받혀서 버렸다
  (`39fec5b`에 무엇이 잘못됐는지 적어뒀다). 하이쿠로 읽는 건 정확도 확인까지만 했다

## 7. 사이트 접근 — 에이전트가 직접 가지 말 것

`champs.pokedb.tokyo/robots.txt`가 **ClaudeBot과 Claude-SearchBot을 이름으로 전면
차단**하고 서버가 실제로 집행한다(403). `blog.naver.com`도 같고, 주석으로 AI 학습
목적 접근 금지를 명시한다. 그래서 `scripts/fetch_usage.py`와 `fetch_articles.py`는
**hk가 돌리는 도구**로 만들어져 있다 — 위장하지 않는 User-Agent, 1.5초 간격, 캐시,
이어받기, 그리고 손으로 저장한 페이지만 파싱하는 `--from-cache`.

`scripts/fetch_article_bodies.py`는 호스트마다 robots를 이 에이전트와 ClaudeBot
양쪽으로 확인하고 거부하는 곳은 건너뛴다. pokesol과 hatena와 note는 허용한다.
