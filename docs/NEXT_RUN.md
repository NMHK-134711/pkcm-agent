# 인수인계 — 2026-09-08 저녁, 개인PC 작성

랩실PC 앞으로. **오늘 엔진이 또 바뀌었다. 어제 끝낸 253파티 라운드로빈을 포함해
모든 승률 수치가 다시 낡았다.** 그러니 이 문서는 "다시 재라"가 아니라 **"재기 전에
기술 전수 검사를 끝내라"** 다.

## 0. 기술 전수 검사 — 497종 전부 끝났습니다 (랩실, 09-08)

```
python scripts/mechanic_check.py     ->  498/498 behaved
```

**챔피언스 표준 기술 497종 중 검사가 없는 기술: 0.** 전부 실제 배틀을 만들어
실제 턴 루프로 돌리고, 결과를 봅니다. 기술마다 해당되는 것을 전부 확인합니다:

| 무엇을 | 어떻게 |
|---|---|
| 동작 | 코드 효과 136종은 손으로 쓴 시나리오, 선언 효과는 캐스팅 후 선언과 대조 |
| **위력·타입·분류** | 데미지가 선언된 위력으로 계산한 16개 굴림 안에 드는지 (엔진 밖에서 따로 계산) |
| **부가효과 확률** | 200회 캐스팅해 실제 발동률이 선언된 확률의 3σ 안인지 |
| **지속시간** | 조건이 데이터가 말한 턴에 정확히 끝나는지 |
| 플래그 | 흡혈·반동·연속기·차지·일격사·교체·급소율·방어관통·해동·추락 |

급소율은 표본으로 1/8과 1/24를 가르기 어려워 다르게 잽니다: **기충전으로 2단계를
얹으면 급소율 2 기술은 4단계 = 전탄 급소**, 무시되면 3단계 = 절반.

### 이 라운드에서 찾아 고친 엔진 버그 여덟

| 기술 | 무엇이 틀렸나 | 필드 |
|---|---|---|
| **앙코르** | 잡은 인덱스를 `legal_actions`가 안 읽음 — 4턴 내내 자유 선택 | 76파티 |
| **킹실드 계열 6종** | 벌칙이 `after_damage`에 걸림 — 방어 성공은 데미지를 없애므로 훅이 안 옴 | 27파티 |
| **떨어뜨리기·중력·뿌리박기** | 접지 판정 통과 후 타입표가 Flying에 0 반환 | 4파티 |
| **불사르기** | 소문자 타입에 대문자 `"Fire"` 비교 — 불꽃타입 전부가 자기 기술을 거부 | 1파티 |
| **열불내기·분함의발구르기** | 방어에 막힌 것이 실패로 기록 안 됨 | 2파티 |
| **승부굳히기** | Counter 원장만 봐서 반동·해저드를 못 봄 | 2파티 |
| **배수의진** | 선언된 5랭크업을 핸들러가 한 번 더 적용 → 전부 +2 | 0 |
| **페어리록** | 거는 사람만, 그것도 영구히 가둠. room은 아무도 안 읽음. 지속시간도 하드코딩 | 0 |
| **신비의부적·와이드가드·패스트가드** | 지속시간 표에 없어 **해저드처럼 층으로 취급 → 영구 지속**. 영구 신비의부적은 남은 배틀 내내 상태이상 면역 | 0 |

전부 회귀 테스트로 고정(780 통과).

### 검사 자체에 대해

**통과하던 테스트가 두 번 거짓말했습니다.** 앙코르 테스트는 볼라틸만 봤고(그게
유일하게 작동하던 부분), 정직성 표는 "카운터만 깎는 핸들러도 핸들러"라 실드 6종을
통과시켰습니다. 그리고 **제가 쓴 검사 7개도 같은 병**이었습니다 — 표식이 생겼는지만
보고 결과를 안 봤습니다(송전·기충전·가드셰어·파워셰어·록온·매직룸·파워트릭).
전부 결과를 보도록 고쳤습니다.

**시나리오가 틀린 게 20회 이상**이었고 대부분 함정표에 있던 것들입니다. 새로 추가:

| 함정 | 실제로 일어난 일 |
|---|---|
| 성별 미기재 | 헤롱헤롱이 "성별 불명"을 거부 — 기술이 안 도는 것과 구분 불가 |
| 위력 상한 | 히트스탬프 두 팔이 둘 다 무게비 상한이라 실제로는 타입 배수를 재고 있었음 |
| 문턱 오설정 | 분풀이는 1.33배가 정상 — 두 배로 만드는 하락이 공격도 2/3로 깎음 |
| 턴 안에서 소멸 | 풀죽음은 턴 끝에 지워져 볼라틸로는 절대 안 잡힘. 로그를 봐야 함 |
| 대조군이 대상을 죽임 | 성묘 두 팔이 42로 같았던 건 목숨걸기가 상대를 42까지 깎아둬서 |
| 프로브가 대상을 재시전 | 지속시간 측정이 매 턴 그 기술을 다시 걸어 영원히 안 끝남 |

### 남은 것

기술 쪽은 닫혔습니다. 특성·도구 쪽도 **0-B**에서 닫혔습니다. **§4의 라운드로빈
재실행과 말단 평가 절제 실험이 가능합니다.** 다만 그 수정들로 엔진이 또 바뀌었으므로
`runs/field_rr_800.jsonl`(64.4% 진행분)은 **낡았습니다** — 새 `--out`으로 다시
시작해야 합니다.

## 0-B. 특성·도구·메가스톤 전수 검사 — 끝났습니다 (랩실, 09-08 밤)

```
python scripts/effect_check.py
  mega stones: 68/68
  abilities:   226개 등록·도달가능 / 207개가 싱글에서 배틀을 바꿈 /
               16개는 아군이 필요 / 3개는 의도적으로 무효 / 침묵 0
  doubles:     17/17 (파트너 특성 전부, 실제 더블 필드에서)
  items:       73/73
```

방법은 기술 검사와 다릅니다. 기술은 손으로 쓴 시나리오지만, 특성은 **차분기**입니다 —
같은 배틀을 특성을 달고 한 번, 떼고 한 번 돌려 트레이스를 통째로 비교합니다. 특성은
"발동 조건이 안 왔을 때"와 "구현이 없을 때"가 겉으로 똑같기 때문에, 조건 쪽을
**64개 포지션**으로 만들어 놓고 그 중 하나라도 달라지는지 봅니다(접촉·특수·날씨 4종·
상태 4종·흡수 타입·랭크 하락·소리·탄환·급소·저체력·나무열매·도구 든 상대·교체 인/아웃·
기술로 오는 상태·선제기·펀치/파동/물기/베기/연속기/약한기술/반동기·4분의1 체력의 네
타입·풀피의 비행기술·무게 대 무게·고스트에게 노말과 격투·자폭·바람에 날려나감·강철을
독으로·성별 명시·비 속 화상과 햇빛 속 화상·스피드가 한 끗 차이인 위블 둘).

### 차분기 자체가 거짓말을 하고 있었습니다

대조군이 **테스트 대상 말고 두 필드**에서 달랐습니다.

1. 대조군이 성별을 안 물려받았습니다. "성별 명시" 포지션에서 상대가 헤롱헤롱을 걸면
   실험군은 걸리고 대조군은 "성별 불명"으로 실패합니다 — **특성이 뭐든 트레이스가
   달라집니다.** 226개 전부가 이 한 포지션으로 통과하고 있었습니다.
2. 도구를 검사할 때도 대조군이 **같은 도구를 들고** 있었습니다. 즉 도구 73종은
   애초에 비교 자체가 안 되고 있었고, "73/73 통과"는 위 1번이 만들어낸 숫자였습니다.

고친 뒤 침묵이 25개로 드러났고, 그것을 다 닫는 것이 이 라운드의 내용입니다.

### 포지션이 못 만들던 조건 (검사 쪽 버그)

| 무엇이 안 잡혔나 | 왜 |
|---|---|
| 트랜지스터·옹골찬턱 | 스윙 대상이 가브리아스 — **전기 무효**였고, 4번째 스윙 전에 쓰러졌다 |
| 불면·의기양양·마그마의무장 | 한 포지션에서 상태기 4개를 연달아 써서 첫 번째만 들어가고 나머지는 "이미 상태이상" |
| 자력·개미지옥 | 갇힌 상대의 교체가 **불법 행동 예외**가 되고, 차분기가 그 예외를 삼켰다 |
| 재생력·자연회복 | "나가기" 스크립트가 사실은 **들어오기**였다(필드에 잡몹을 세우고 switch) |
| 탈·꼬르륵스위치·마이티체인지 | 잠만보에 얹어서는 폼체인지를 보여줄 수 없다 |
| 정화의소금 | 고스트 기술을 **노말 타입**에게 쏘고 있었다 |
| 리프가드·선파워 | 햇빛과 상태기가, 햇빛과 특수기가 같은 포지션에 없었다 |
| 질풍날개·힐링시프트 | 상대가 이미 느려서 우선도가 순서를 안 바꿨다 |
| 헤비메탈·라이트메탈 | 잠만보 대 잠만보는 무게비 1:1 — 두 배든 절반이든 같은 칸 |

이제 포지션은 71개고, 행동이 불법이 되면 `refused(...)`로 트레이스에 남습니다.

### 실제로 구현이 없던 특성 아홉

| 특성 | 무엇이 없었나 | 합법 보유자 |
|---|---|---|
| **아로마베일** | 도발·앙코르·헤롱헤롱·사슬묶기·트집·회복봉인 6종 거부 | 2 |
| **볼주머니** | 나무열매를 먹으면 최대 HP의 1/3 추가 회복 | 3 |
| **서투름** | 자기 도구를 무효화 — `Context.item_of`(매직룸과 같은 관문)에 넣었다 | 3 |
| **나쁜손버릇** | 접촉당하면 상대 도구를 뺏음 | 4 |
| **픽업** | 그 턴에 소비된 도구를 주움 | 6 |
| **주눅** | 벌레·악·고스트 기술과 위협에 스피드 +1 | 0 |
| **플라워베일** | 풀타입 아군의 상태이상·랭크 하락 차단 (`ally_try_boost` 훅 신설) | 2 |
| **정화의소금** | 고스트 반감만 있고 **상태이상 완전 무효가 없었다** | — |
| **선파워** | 특공 1.5배만 있고 **햇빛 중 매턴 1/8 소모가 없었다** | — |

`INERT`는 11개에서 **4개**로 줄었고 넷 다 이유가 적혀 있습니다: 꿀모으기·도주·볼줍기는
배틀 밖 효과(그리고 합법 보유자 0), 먹보는 문턱을 1/4에서 1/2로 옮기는 특성인데
**M-B의 합법 나무열매 28종에 1/4 문턱짜리가 하나도 없습니다**(오랭·자뭉은 1/2, 나머지는
상태·PP·반감).

### 더블 전용 17종은 진짜 더블 필드에서

싱글에서 보이지 않는 것이 "구현 없음"이 아님을 보이려면 아군이 있어야 합니다.
`scripts/effect_check.py --doubles`가 6마리 중 4마리 더블을 실제로 열고, 17종
각각에 대해 **숫자까지** 봅니다.

배터리 특수기 x1.297·물리기 x1.000 / 파워스폿 양쪽 x1.30 / 프렌드가드 아군 x0.747·
본인 x1.000 / 플러스·마이너스 x1.459(짝이 없으면 x1.000) / 치유의마음 600판에 확률
0.30±3σ / 대접 최대 HP의 1/4 / 기묘한약은 올린 랭크까지 지움 / 협연은 내린 랭크까지
복사 / 공생은 아군이 열매를 쓴 즉시 건너감 / 리시버·과학의힘은 쓰러진 아군의 특성을
승계 / 스위트베일은 버섯포자와 하품 둘 다 / 텔레파시는 아군 파도타기 0 피해 /
스크루지느러미·굳건한신념은 나를따르라를 무시하고 조준한 쪽을 때림 / 플라워베일은
풀타입 아군만.

### 회귀

- `pytest` **802 통과, 3 스킵** (특성 13개·더블 7개 신규)
- `python scripts/mechanic_check.py` **498/498** (엔진이 또 바뀌었으므로 재확인)

## 0-C. 오로라베일 — hk 질문에서 나온 다섯 (랩실, 09-08 밤)

질문은 하나였습니다: "눈 내릴 때만 가능해야 하고, 특수·물리 둘 다 반감시켜야 하는데."
**반감은 맞았고, 눈 요구는 없었습니다.** 그리고 같이 본 김에 넷이 더 나왔는데, 셋은
리플렉터·빛의장막도 같이 틀렸습니다. **다섯 전부 저장소에 이미 들어있는 기술 데이터
`desc`에 적혀 있는 내용**입니다 — 엔진이 자기 데이터와 어긋나 있었습니다.

| 무엇 | 실측 (전 → 후) |
|---|---|
| **눈에서만** | 맑음·비·모래·햇빛에서도 전부 발동 → 눈에서만 |
| 물리·특수 양쪽 반감 | x0.500 / x0.500 — **원래 정상** |
| **리플렉터와 중첩** | 둘 다 걸면 **x0.25** → 겹쳐도 x0.5 (베일이 있으면 나머지 둘은 손 뗌) |
| **급소** | 급소도 반감 x0.500 → 급소는 그냥 통과 x1.000 |
| **깨뜨리다·사이코팽** | 스크린을 전혀 안 부숨 → 자기 데미지 계산 **전에** 해제 |
| **더블 배수** | x0.500 → **x0.667** (2732/4096, 세 스크린 모두) |

레이징불은 부수기는 했지만 `_after_effects`에서 돌아 **자기 공격이 그 스크린에 먼저
반감된 뒤** 부수고 있었습니다. 셋 다 `moveeffects.break_screens` 하나로 모으고
데미지 루프 앞에서 부릅니다.

빛의점토 8턴은 원래 정상이었습니다(5 → 8 실측).

### 검사가 이걸 왜 못 잡았나

지속시간 프로브는 **"캐스팅했는데 안 걸렸으면 `inconclusive`"** 로 끝나고,
`inconclusive`는 통과로 셉니다. 눈 요구를 넣자마자 오로라베일 프로브는 조용히
inconclusive가 됐고 `498/498`은 그대로였습니다. 이제 `_NEEDS_FIELD`가 프로브 전에
필드를 만들어 주고, 오로라베일·깨뜨리다·사이코팽·레이징불은 손으로 쓴 검사가
따로 있습니다(숫자까지: 눈 밖 거부, x0.486/x0.500, 중첩 17 대 17, 급소 x1.000,
스크린 뚫고 110/110/110).

`pytest` **810 통과**(스크린 8개 신규), `mechanic_check` 498/498.

## 0-D. 설명문 전수 검사 — 끝났습니다 (`scripts/clause_check.py`)

오로라베일 다섯 개가 전부 `desc` **산문**에 있었고 필드만 읽는 검사기는 그걸 볼 수
없었습니다. 그래서 검사 단위를 **조항(문장)** 으로 바꿨습니다.

```
497개 설명문 = 1022문장 = 중복 제거 621조항

python scripts/clause_check.py
  226 families, 610/621 clauses claimed, 1006/1022 sentences
  11 clauses name only mechanics this format does not have (recorded, not checked)
  0 still unclaimed
```

조항 하나를 그 조항을 가진 **모든 기술**에 대해 실제 배틀로 검증합니다. 끝에 **아무도
안 맡은 조항을 전부 출력**하고, 지금 그 수가 0입니다. 이 포맷에 없는 것만 언급하는
11개(스카이드롭·전천후우산·매직코트·자연의은혜·블루오브)는 **이름을 뽑아 포맷의
기술·도구 목록과 대조해서** 따로 분류합니다 — 넘어간 게 아니라 못 잰다고 적어둔
것입니다.

더블 전용 12조항(나를따르라·패스트가드·와이드가드·도우미·앵콜파티·순서바꾸기·
지압·꽃가루경단·용의화살·페인트)은 이 스크립트 안에서 **실제 더블 필드**를 열어
검증합니다.

### 엔진 버그 72개

전부 "선언된 필드는 구현했고 문장의 나머지 절반은 아무도 안 읽음" 형태입니다.
가장 큰 것부터:

| 무엇 | 실제로 일어나던 일 |
|---|---|
| **필드 4종 전부** | 데미지 배수만 구현 — 일렉트릭이 잠을, 미스트가 상태이상·혼란을, 사이코가 선제기를 못 막고 그래스는 회복을 안 함. 그래스의 지진 반감도 없음 |
| **미래예지·웨더볼·불카의뿔** | 미래예지는 **구현이 아예 없었고**, 나머지 둘은 **타입이 안 바뀜** |
| **대타출동** | `authentic` 플래그를 봤는데 데이터는 `bypasssub` — **소리기술이 한 번도 대타를 못 뚫었음** |
| **프로텍트 연쇄** | 킹실드 계열 6종이 한 시전에 두 번 굴려 첫 사용부터 2/3 실패; 인내·패스트가드·와이드가드는 연쇄에 없었음 |
| **날씨 재설치** | 같은 날씨 재설치가 공짜 갱신 (8기술) |
| **가두기 12종** | 건 쪽이 교체해도 안 풀림 |
| **부리포·목조르기·뿌리박기·섀도바인드·클리어스모그** | 각각의 존재 이유가 통째로 미구현 |
| **위력 조항 4개** | 베놈쇼크·바르바렛지·객기·G의힘이 **한 번도 2배가 안 됨** |
| **베턴터치** | 전달 목록의 `gastroacid`가 실제 볼라틸 이름 `abilitysuppressed`와 안 맞음 |
| **용의화살** | `smartTarget`을 안 읽어 더블에서 한 명한테 두 발 |
| **킹실드** | 변화기까지 막음(막으면 안 됨) |
| **일격필살 4종** 옹골참 무시 / **급소 3종** 전투무장 관통 / **번개·폭풍·눈보라** 날씨 명중률 없음 / **중력** 회피 보정·마그넷라이즈 해제 없음 / **몸부림** 돌머리가 막음 / **트림·플링·힐링위시·꼬리자르기·포르터가이스트·뱉어내기** 조건 없이 성공 | |

### 검사기 쪽에서 고친 것

- `mechanic_check`의 **`inconclusive`가 통과로 세지고 있었습니다** — 18개가 조용히
  미측정. 이제 `??`로 표시되고 실패 처리되며, 18개 전부 측정됩니다.
- 데이터가 선언한 볼라틸이 **source 없이** 걸려서 "누가 걸었나"를 묻는 효과가
  거부할 수 없었습니다.
- 기술을 `VARIABLE_POWER`에 넣으면 생성 검사에서 빠져 **498이 조용히 495가 되는**
  일이 두 번 있었습니다. 지금은 넷 다 손으로 쓴 검사가 있습니다.

### 도발·트집 — hk 지적으로 고침

실제 게임에서는 두 경우가 다릅니다.

- **이미 도발 상태**: 변화기를 고를 수 **없습니다**(게임이 "도발 상태라 쓸 수 없다"고
  띄웁니다). 이제 `legal_actions`가 변화기를 빼고, 변화기밖에 없으면 몸부림이 나옵니다.
- **변화기를 고른 턴에 도발이 먼저 들어옴**: 그 턴은 **그냥 날아갑니다**. 이건 원래
  맞게 동작하고 있었고, 그대로 둡니다.

트집도 같습니다 — 직전에 쓴 기술이 목록에서 빠집니다. 앙코르·사슬묶기는 원래부터
`legal_actions`가 읽고 있었고, 이 둘만 빠져 있었습니다. 탐색과 엔진이 같은 목록을
보게 된 것이 요점입니다.

### 회귀

```
pytest                            825 passed, 3 skipped
python scripts/mechanic_check.py  498/498, 미측정 0
python scripts/effect_check.py    226 abilities 0 silent / 73 items / 68 stones / 17 doubles
python scripts/clause_check.py    226 families, 0 unclaimed
```

## 0-E. 위임을 전부 걷어냈습니다 — 466/466 (`scripts/coverage_audit.py`)

§0-D의 "0 unclaimed"는 생각보다 약한 문장이었습니다. **조항을 누가 맡았다(claimed)는
것과, 맡은 검사가 그 문장을 실제로 쟀다는 것은 다른 말**입니다. 패밀리 아홉 개는
조항을 `mechanic_check`의 같은 기술 검사를 **다시 돌리는 것으로** 맡고 있었고, 그
검사가 이 문장을 덮는다는 건 제 판단이지 측정이 아니었습니다.

**드래곤옐**이 그 틈이었습니다. "급소율을 1랭크, 아군이 드래곤이면 2랭크 올린다"는
문장을 `"Raises the "` 패턴이 가져갔고, 그 검사는 기술 데이터의 `boosts` 필드를
잽니다. 급소 단계는 능력치 랭크가 아니므로 **잴 게 없었고, 조용히 통과**했습니다.
엔진은 기합가둠의 볼라틸을 붙이고 있어서 노말 아군에게도 +2를 주고 있었습니다.

`scripts/coverage_audit.py`가 그 구분을 셉니다. 시작점은 위임 280개(A 2 / B 87 /
C 191)였고, 지금은 **0**입니다.

```
python scripts/coverage_audit.py
  466 moves carry an effect sentence; 466 are checked sentence by sentence
  docs/UNVERIFIED_MOVES.md   (목록이 비어 있음)
```

31개는 설명이 없거나 "No additional effect."뿐인 순수 데미지기라 제외했고, 전천후우산
조건 5문장은 그 도구가 이 포맷에 없어 **도달 불가로 기록**했습니다(`needs_item`).

### 엔진 버그 20개

§0-D와 같은 형태가 아닙니다. 여기 나온 것들은 **필드는 다 구현돼 있는데 문장이
필드보다 더 말하고 있던** 경우입니다.

| 무엇 | 실제로 일어나던 일 |
|---|---|
| **프리즈드라이** | 물타입에 반감. 그 기술을 채용하는 유일한 이유가 1/4로 동작 |
| **플라잉프레스** | 비행 상성이 안 합쳐져 풀타입에게 반감 |
| **대타출동** | 데미지만 막음. 도깨비불이 인형을 뚫고 화상을 입힘 |
| **스피드스왑** | 능력치가 아니라 **랭크**를 교환 — `mechanic_check` 검사에도 "랭크를 바꾼다"고 적혀 있어 **틀린 규칙이 두 군데에서 서로 동의**하고 있었음 |
| **백귀야행·요정의목소리** | `secondary: {chance: 100}`뿐이라 100% 확률로 아무것도 안 함 |
| **클랑그라스소울** | 5개 랭크를 HP 대가 없이 공짜로 |
| **힘껏펀치** | 맞아도 그대로 발동 (-3 우선도 기술의 존재 이유) |
| **셸암즈** | 구현 자체가 없음. 항상 특수, 접촉 없음 |
| **뿌리박기** | 교체를 막지 않음 |
| **미러타입** | 타입 없는 대상에 IndexError로 배틀이 죽음 |
| **화상** | 물리 *데미지*가 아니라 공격 *랭크*만 반감 → 바디프레스·속임수가 안 깎임 |
| **충전** | 소비되지 않아 한 번 쓰면 그 뒤 전기기 전부 2배 |
| **자이로볼** | 공식의 `+1` 누락 |
| **꼬리자르기** | 비용과 인형 HP가 한 변수라 인형이 두 배 |
| **치유소원** | 함정보다 나중에 회복 → 교체 요원이 스텔스록에 먼저 맞음 |
| **볼가득넣기** | 나무열매 없이도 행동 목록에 나옴 |
| **안개제거·배턴터치·변신·치유파동·파워트릭·독압정·막말내뱉기·변덕레이저** | 각각 문장의 절반씩 |

### 남은 위험 — 여기부터는 못 재는 것

- **잘못된 이유로 통과하는 probe는 여전히 안 보입니다.** 466/466은 "모든 문장에 그
  문장을 읽고 쓴 probe가 서 있다"는 뜻이지 "엔진이 맞다"가 아닙니다.
- 기준은 **Showdown의 `desc` 산문**입니다. Champions와 다른 지점이 있으면 그건
  이 검사로 안 잡힙니다. 드래곤옐도 hk가 알아봐서 나온 것입니다.
- 기술 **상호작용**은 여전히 사각지대입니다. 문장 하나씩만 봅니다.
- 더블 커버리지는 얇습니다.
- 확률 검사 허용치는 3σ에 하한 5%p입니다.

### 회귀

```
pytest                            850 passed, 3 skipped
python scripts/mechanic_check.py  498/498, 미측정 0
python scripts/effect_check.py    226 abilities 0 silent / 73 items / 17 doubles
python scripts/clause_check.py    241 families, 0 unclaimed, 4 cannot arise here
python scripts/coverage_audit.py  466/466
```

### 파티 생성에서 뺀 기술

`legality.NOT_WORTH_A_SLOT` = {아로마미스트}. hk: 아무도 안 씁니다. 엔진은 정상
실행하고 `team_errors`도 그대로 통과합니다 — 생성기와 변이 연산자만 안 뽑습니다.
이건 포맷에 대한 판단이지 엔진 능력에 대한 판단이 아니라서 `scope`가 아니라
`legality`에 있습니다.

## 1. 오늘 고친 것 — 커밋 셋

| 커밋 | 무엇 |
|---|---|
| `8fccf6f` | **사전확률이 변화기에 타입 상성을 곱하고 있었다.** 빛의장막이 악 타입 상대 0.045, 고스트 상대 0.900 — 같은 벽을 세우는데 20배. 인파이트 대비 107배 아래라 가지가 잘려 탐색이 보지도 못했다. 이제 고정값 0.45 |
| `545b95b` | 실측 하네스 신규 |
| `1911a4f` | **기술 다섯 개가 데이터대로 동작하지 않았다** (아래) |

### `1911a4f`이 고친 다섯

- **배턴터치** — `selfSwitch`가 `True`가 아니라 `'copyvolatile'`인데 truthy로만 읽어
  유턴 경로로 갔다. 랭크가 교체와 함께 지워졌다. 필드 **10파티**가 들고 있다
- **꼬리자르기** — 대타출동을 만들자마자 `return`해서 교체 분기에 못 갔다. 3파티
- **막말내뱉기** — 하락이 쇼다운의 `onHit`에 있어 `boosts` 필드가 없다. 아무것도
  안 했다. 3파티
- **절대영도** — `ohko`가 가위자르기는 `True`, 절대영도는 **타입 이름 `'Ice'`**.
  플래그로 읽어 얼음타입을 일격사시켰다. 3파티
- **리벤지** — `VARIABLE_POWER` 표에 누락. 쌍둥이인 눈사태는 처음부터 있었다

그리고 공통 원인 하나: **공식을 건너뛰는 데미지가 `hurtthisturn` 원장에 안 실렸다**
(지구던지기·나이트헤드·용의분노·소닉붐·거대이빨). 카운터가 지구던지기에 "실패"로
답했고, 눈사태가 그걸 맞고도 위력 60이었다.

## 2. 정상으로 **확인된** 것 — 다시 재지 말 것

실행해서 본 것만 적는다.

- 리플렉터(물리 0.52배), 빛의장막(특수 절반), 지속 5턴, 빛의점토 8턴 — 다만
  급소·중첩·더블 배수는 §0-C에서 틀린 것으로 드러났다
- 청개구리 + 인파이트 (3연타에 방어·특방 **+3**)
- 목숨걸기, 미러코트(특수 반사 O / 물리 실패 O), 와이드가드(더블), 죽기살기,
  어시스트파워(랭크 0에 10 → +3에 246), 풀묶기(46.8kg 24 → 460kg 54), 눈사태
- 비바라기, 소원, 저주(고스트/비고스트 양쪽), 섀도다이브의 방어 관통
- 기술 우선도 35개 전부 (09-06 감사)

## 3. 왜 지금 재면 안 되는가

hk의 목표는 "웬만한 파티에 대응방법이 있는, 저점이 높은 파티"다. 어제 라운드로빈에서
**파티의 변화기 비중과 승률의 상관이 −0.457**로 단조롭게 나왔다(변화기 3칸 56.8% →
14칸 43.5%). 이게 "변화기 파티가 실제로 약하다"인지 "우리 쪽이 변화기를 못 다룬다"인지
갈라야 하는데, 오늘 그 원인이 최소 셋으로 쪼개졌다.

1. **사전확률** — 고쳤다. 50파티 재측정에서 변화기 많은 파티가 적은 파티 대비
   +3.5%p 올라갔다(t≈2.1, 시사적이지 확립은 아님). 전체 기울기는 −0.50 → −0.47로
   거의 안 움직였다. **원인의 일부지 주범이 아니다**
2. **기술 미구현** — 배턴터치·막말내뱉기·꼬리자르기가 아예 안 돌았다. 그리고 이건
   정확히 변화기 많은 파티에 들어간다. 고쳤지만 **나머지 488종은 안 봤다**
3. **말단 평가** — 물량 세기는 이 탐색이 도달하는 2.8턴 안에서 벽·설치기·축적을
   점수로 셀 수 없다. `evaluate.py`가 스스로 적어놓은 한계다

**2가 끝나기 전에는 3을 잴 수 없다.** 그래서 전수 검사가 먼저다.

## 4. 그 다음 — 검사가 끝나면

1. 라운드로빈 재실행. **전체가 아니라 `--opponents 60`** — 아래를 읽을 것.
   개인PC 혼자 7.3시간, `runs/field_rr_sub60.json` (09-09 진행 중)
2. `python scripts/field_report.py runs/field_rr_sub60.json` — 저점 집계와 반분 검증
3. 변화기 상관이 얼마나 남는지 본다. 그게 말단 평가에 대한 깨끗한 질문이 된다
4. `--evaluation pressure` / `blind` 절제 실험 (`8fccf6f`에서 플래그 추가됨).
   50파티 부분집합 명령은 `runs/eval_ablation_entrants.txt`에 있다

### 전체 라운드로빈을 돌리지 말 것 — 26시간이 사는 게 없다

완주한 v1로 실측했다. **이 측정이 만드는 건 순위가 아니라 후보군이다.**

| 순위 | 통계적으로 분리되는 지점 |
|---|---|
| 1위 | 9위부터 — 사실상 "1~8위 중 하나" |
| 5위 | 22위부터 |
| 100위 | 171위부터 |

반분 재현성도 같다. 절반씩(파티당 504게임) 독립으로 순위를 매기면 **상위 5개 중
2개, 상위 10개 중 7개**만 겹친다.

`--opponents 60`은 파티마다 60명씩만 만난다 — 15,156대진, 6~7시간, 그리고 전체
결과의 상위 10개를 **7/10** 재현한다. 전체가 자기 자신을 재현하는 정확도와 같다.
남은 20시간은 특정 동전던지기 한 벌과의 일치를 사는 데 쓰였을 뿐이다.

부분그래프는 기계가 아니라 **고정 시드**에서 나오므로, 두 기계가 같은 `--out`을
쓰면 같은 그래프를 자르고 `--reverse`로 마주볼 수 있다. **양쪽 다 `--opponents`를
넣어야 한다** — 한쪽만 넣으면 다른 스케줄이라 마주보기가 성립하지 않는다.

**저점(CVaR)은 라운드로빈으로 못 잰다.** 쌍당 4게임에서 "최악의 1/4"은 운으로
채워진다 — 반분 검증으로 편향 **+39.6%p**를 실측했다(고른 게임으로 1.7%, 다른
절반으로 41.3%). 라운드로빈의 역할은 후보 선별까지고, 저점은 게임을 꼬리에
몰아주는 `scripts/party_floor.py`로 재야 한다. 그 기본값은 46파티 시절 것이라
253필드에서는 시드 패스만 1명당 2,016게임이다 — `--candidates 5 --seed-games 4
--budget 12000`이 대략 10시간.

## 5. 명령 모음

```bash
# 기술 실측 검사
python scripts/mechanic_check.py

# 특성·도구·메가스톤·더블 실측 검사
python scripts/effect_check.py
python scripts/effect_check.py --doubles -v

# 설명문(조항) 전수 검사
python scripts/clause_check.py
python scripts/clause_check.py bigroot     # 한 계열만

# 253파티 라운드로빈 (개인PC 19워커 분당 40대진, 랩실 10워커 분당 10대진)
python scripts/tournament.py --parties data/champions/parties_field.json \
    --repeats 2 --search-iterations 800 --preview-iterations 3200 \
    --opponents 60     --workers <물리 코어> --out runs/field_rr_sub60.json   # 두 번째 기계는 뒤에 --reverse
```

두 기계가 스케줄 양 끝에서 마주보고 돌린다. `--reverse` 없이 둘이 같이 돌리면
통째로 중복 계산한다. `runs/field_rr_*.jsonl`은 `merge=union`이라 pull이 자동 병합한다.
`--parties`·`--repeats`·`--search-iterations`·`--preview-iterations`는 양쪽이 같아야
하고 `--workers`만 기계에 맞춘다.

## 6. 사이트 접근 — 에이전트가 직접 가지 말 것

`champs.pokedb.tokyo/robots.txt`가 ClaudeBot과 Claude-SearchBot을 이름으로 전면
차단하고 서버가 집행한다(403). `blog.naver.com`도 같다. `scripts/fetch_usage.py`와
`fetch_articles.py`는 **hk가 돌리는 도구**로 만들어져 있다.
